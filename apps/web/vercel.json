{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd shared-types && npm install && npm run build && cd .. && npm install && npm run build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["iad1"]
}
